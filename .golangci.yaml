version: "2"
run:
  timeout: 5m
  tests: true

linters:
  enable:
    - govet
    - staticcheck
    - ineffassign
    - unused
    - gocritic
    - errorlint
    - errname
  disable:
    - errcheck
formatters:
  enable:
    - gofumpt
    - goimports
    - gci
