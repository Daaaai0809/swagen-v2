GET:
  operationId: getTestData
  summary: this is test get api endpoint definition
  description: Get エンドポイントAPI定義のテスト
  tags:
  - Tests
  parameters:
  - in: path
    schema:
      $ref: ../model/testModel.yaml#/properties/id
  - in: query
    schema:
      type: string
      format: email
  responses:
    "200":
      description: 成功レスポンス
      content:
        application/json:
          schema:
            $ref: ../schema/testResponse.yaml#/TestResponse
    default:
      description: デフォルトレスポンス(エラー)
      content:
        application/json:
          schema:
            type: string
